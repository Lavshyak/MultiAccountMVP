Example web API with multiple parallel accounts (but 1 account per api method (single Identity in User)) (IdentityUser). 2 accounts, 2 cookies. authorization with policies.

asp.net 8, identity, ef core in memory
